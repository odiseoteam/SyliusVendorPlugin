{% import '@SyliusAdmin/shared/helper/translations.html.twig' as translations %}

{% set form = hookable_metadata.context.form %}
{% set prefixes = hookable_metadata.prefixes %}

<div class="border">
    {{ translations.with_hook(form.translations, prefixes, null, {accordion_id: 'vendor-translations', accordion_flush: true}) }}
</div>
