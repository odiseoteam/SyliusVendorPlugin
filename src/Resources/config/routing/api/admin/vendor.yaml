odiseo_sylius_vendor_plugin_api_admin_vendor:
    resource: |
        identifier: slug
        alias: odiseo_sylius_vendor_plugin.vendor
        section: admin_api
        serialization_version: $version
        criteria:
            slug: $slug
    type: sylius.resource_api
