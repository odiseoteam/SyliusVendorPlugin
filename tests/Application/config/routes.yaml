odiseo_sylius_vendor_shop:
    resource: "@OdiseoSyliusVendorPlugin/config/shop_routing.yaml"
    prefix: /{_locale}
    requirements:
        _locale: ^[a-z]{2}(?:_[A-Z]{2})?$

odiseo_sylius_vendor_admin:
    resource: "@OdiseoSyliusVendorPlugin/config/admin_routing.yaml"
    prefix: /admin
